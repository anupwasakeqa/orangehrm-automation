### CI Artifacts

After every successful or failed GitHub Actions execution, the workflow uploads test execution artifacts for debugging and reporting.

The CI workflow currently generates the following artifacts:

```text
orangehrm-extent-report
orangehrm-html-test-reports
orangehrm-test-screenshots
orangehrm-test-videos
